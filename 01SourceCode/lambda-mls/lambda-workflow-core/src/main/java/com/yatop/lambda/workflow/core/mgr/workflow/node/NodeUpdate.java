package com.yatop.lambda.workflow.core.mgr.workflow.node;

import org.springframework.stereotype.Service;

@Service
public class NodeUpdate {

/*    @Autowired
    NodeMgr nodeMgr;

    //cond:nodeId, set:名称、X坐标、Y坐标、警告消息、节点状态、注释、概要、描述
    public WfFlowNode updateNode(WorkflowContext workflowContext, WfFlowNode node) {
        nodeMgr.updateNode(node, workflowContext.getOperId());
        return nodeMgr.queryNode(node.getNodeId());
    }*/
}
